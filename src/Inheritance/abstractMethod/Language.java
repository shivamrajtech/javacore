package Inheritance.abstractMethod;
//he abstract class in Java cannot be
// instantiated (we cannot create objects of abstract classes).
// We use the abstract keyword to declare an abstract class. For example,
 abstract class Language {
    public void display(){
         System.out.print("This is Java Programming!");
     }
     abstract void displayDemo();

}
